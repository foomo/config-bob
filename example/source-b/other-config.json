{
    "hello" : {
        "test" : {
            "user" : "{{.other.config.hello.test.user}}",
            "password" : "{{.other.config.hello.test.user}}"
        },
        "payment" : {
            "token": {{ secret_json "secret/payment.token" }}
        }
    }
}
